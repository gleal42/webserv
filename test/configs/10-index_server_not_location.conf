# ---------------------------------------------------------------------------- #
#                        Index server=set location=unset                       #
# ---------------------------------------------------------------------------- #

server {
	listen 80;

	root /mid/;
	index cgi_php_multi.html;
	server_name first;
	location /cgi/ {
		limit_except GET POST;
	}
}